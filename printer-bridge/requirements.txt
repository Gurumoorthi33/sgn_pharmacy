fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
requests>=2.31
pillow>=10.0
# USB send path (Android/Termux + Sunmi). On Termux also run:
#   pkg install libusb termux-api
# On Debian/Ubuntu: sudo apt install libusb-1.0-0
pyusb>=1.2.0
